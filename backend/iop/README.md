# AliExpress IOP SDK

Place the official AliExpress `iop` SDK files in this directory.

The application expects this import to work:

```python
import iop
```

After adding the SDK files, the folder usually contains files such as `__init__.py` and the client/request classes provided by AliExpress.
